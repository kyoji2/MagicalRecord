#import "Kiwi.h"

// Project
#import "MagicalRecord.h"

SPEC_BEGIN(MagicalRecordSpec)

describe(@"MagicalRecord", ^{
	beforeAll(^{
		// Occurs once
	});
	
	afterAll(^{
		// Occurs once
	});
	
	beforeEach(^{
		// Occurs before each enclosed "it" block
	});
	
	afterEach(^{
		// Occurs after each enclosed "it" block
	});
    
    pending(@"should really test something — sadly, you've only just created this spec, so it's currently unimplemented", nil);
});

SPEC_END
